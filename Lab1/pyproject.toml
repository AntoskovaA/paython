[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.build_meta"

[project]
name = "finance-tracker"
version = "0.1.0"
description = "Financial Tracker - Laboratory Project for Professional Python course"
requires-python = ">=3.11"
authors = [
    { name = "Антошкова Аліна" }
]
readme = "README.md"

[project.scripts]
finance-tracker = "finance_tracker.main:main"