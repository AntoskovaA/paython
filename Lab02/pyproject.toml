[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.build_meta"

[project]
name = "finance-processor"
version = "0.1.0"
description = "Data structures and functional decomposition for financial tracker"
requires-python = ">=3.11"

[project.scripts]
finance-processor = "finance_processor.main:main"